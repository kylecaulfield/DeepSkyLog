# Admin password for accessing write endpoints.
ADMIN_PASSWORD=changeme

# Port the HTTP server listens on.
PORT=3000

# Directory where uploaded images are stored (relative or absolute).
UPLOAD_DIR=./uploads
